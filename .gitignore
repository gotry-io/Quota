.DS_Store
.env
.env.*
!.env.example

# Swift / Xcode
.build/
.swiftpm/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
apps/ios/Local.xcconfig

# JavaScript / TypeScript
node_modules/
dist/
.svelte-kit/
apps/web/static/schema/
# Leftover local Wrangler state from the retired Workers runtime.
.wrangler/
coverage/

# Build outputs
bin/
target/

# Runtime data
data/
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.log
tmp/

# IDEs
.idea/
.vscode/

# pnpm content-addressable store (cloud agent sandboxes drop it in the repo root)
.pnpm-store/

# Agent worktrees (nested checkouts confuse Biome and must never be committed)
.claude/worktrees/

# Self-hosted Relay secrets (Compose env_file and ${TUNNEL_TOKEN} substitution)
deploy/relay/relay.env
deploy/relay/.env
