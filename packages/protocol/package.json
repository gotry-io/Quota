{
  "name": "@gotry-io/quota-protocol",
  "version": "0.0.3",
  "license": "MIT",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc -p tsconfig.json",
    "generate:schema": "node --experimental-strip-types scripts/generate-json-schemas.ts && pnpm exec biome format --write schema",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "4.4.3"
  }
}
